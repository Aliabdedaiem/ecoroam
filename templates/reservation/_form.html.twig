{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="main-btn primary-btn">{{ button_label|default('BOOK') }}<i
                                        class="fas fa-paper-plane"></i></button>
{{ form_end(form) }}
